# 로컬 서버 실행 가이드

이 문서는 로컬 개발 환경에서 부동산 관리 시스템 서버를 실행하는 방법을 안내합니다.

## 실행 단계

1. **터미널(CMD) 실행**
   `Win + R` 키를 누르고 `cmd`를 입력하여 명령 프롬프트를 엽니다.

2. **프로젝트 디렉토리로 이동**
   ```cmd
   cd c:\code1\realestate-management
   ```

3. **가상환경 활성화**
   ```cmd
   venv\scripts\activate
   ```
   *성공하면 프롬프트 앞에 `(venv)` 표시가 나타납니다.*

4. **서버 실행**
   ```cmd
   python run.py
   ```

## 접속 주소
와이파이 등 네트워크 환경에 따라 접속 주소가 달라질 수 있습니다.

### 1. 항상 고정된 주소 (추천)
서버를 실행 중인 컴퓨터에서 직접 접속할 때는 어떤 환경에서도 변하지 않는 아래 주소를 사용하세요:
- **[http://127.0.0.1:5000](http://127.0.0.1:5000)**

### 2. 가변 주소 (다른 기기 접속 시)
스마트폰이나 다른 노트북에서 접속해야 할 때는 현재 네트워크의 아이피(IP)를 확인해야 합니다.

**확인 방법 A: 서버 로그 확인**
서버 실행 후 터미널에 나타나는 로그에서 확인할 수 있습니다:
```text
🚀 서버 시작: http://172.30.1.64:5000
```

**확인 방법 B: 명령어로 확인**
새 터미널 창을 열고 아래 명령어를 입력하세요:
```cmd
ipconfig
```
- 결과 중 `IPv4 주소` 항목 옆의 숫자(예: 172.30.1.64)를 확인하면 됩니다.

---
> [!NOTE]
> 서버 종료를 원하시면 터미널에서 `Ctrl + C`를 누르세요.
