# 부동산 관리 시스템 환경변수 설정 예시
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요

# 서버 설정
PORT=5000
HOST=0.0.0.0
FLASK_DEBUG=False

# 보안 설정
SECRET_KEY=your-secret-key-here-change-this-to-random-string

# 사용자 권한 설정 (쉼표로 구분)
ALLOWED_USERS=user1,user2,user3
ADMIN_USERS=admin1,admin2

# 보안 옵션
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION=300
SESSION_TIMEOUT=28800
MAX_REQUESTS_PER_MINUTE=100

# Google Sheets 설정
SPREADSHEET_ID=1D14iWPeTuHAMf9m_LrtsILYEd2Z8dpjAbIfpx-WR8eY
SERVICE_ACCOUNT_FILE=../config/service_account.json
SHEET_DOWNLOAD_INTERVAL=5

# 주택매물장 시트 (Sheets → Supabase 동기화)
# SK부동산 주택매물장. 미설정 시 아래 기본값 사용.
HOUSING_SHEET_ID=1KZ7aLN_Vzfnp0MhnOsJXuCtPtGIPuVj-UaHB2xP7JRs

# 웹훅 (Google Sheets 변경 감지 → Supabase 동기화)
# HTTPS 공개 URL. 설정 시 서버 실행 중 매일 05:00에 웹훅 자동 등록.
WEBHOOK_BASE_URL=https://skrealestate.duckdns.org
# 웹훅 갱신 실행 시각 (선택, 기본 05:00)
# WEBHOOK_RENEW_HOUR=5
# WEBHOOK_RENEW_MINUTE=0

# Naver 지도 API 설정
NAVER_MAPS_NCP_CLIENT_ID=your_naver_maps_client_id
NAVER_MAPS_NCP_CLIENT_SECRET=your_naver_maps_client_secret

# 데이터 경로 설정
DATA_DIR=./data
LISTING_SHEET_FILENAME=상가임대차.xlsx
MAP_CACHE_FILENAME=지도캐시.xlsx

# 캐시 파일 설정
LISTING_CACHE_FILE=./data/cache/listings_normalized.json
GEOCODE_CACHE_FILE=./data/cache/geocode_cache.json

# Supabase DB 연결 (스키마 내보내기·SQL 실행 시 사용)
# Supabase 대시보드 → Settings → Database → Connection string에서 확인
# SUPABASE_URL이 있으면 SUPABASE_DB_PASSWORD만 추가해도 됨
SUPABASE_DB_PASSWORD=your-database-password
# 또는 전체 연결 문자열 사용:
# DATABASE_URL=postgresql://postgres:비밀번호@db.프로젝트ID.supabase.co:5432/postgres

# DuckDNS 설정 (선택사항)
DUCKDNS_DOMAIN=your-domain.duckdns.org
DUCKDNS_TOKEN=your-duckdns-token
DUCKDNS_UPDATE_INTERVAL=300


