# 주택매물 추가 — 권장 진행 순서

기획 문서(`주택매물_기획_검토의견.md`)에 이어 참고용으로 정리했습니다. 코드 수정은 하지 않았습니다.

---

## 기준

- **의존 관계**: “저장소 → 데이터 채우기 → API → 화면” 순으로, 앞 단계가 있어야 다음 단계가 가능하도록 함.
- **검증 가능성**: 각 단계 끝날 때마다 “여기까지 동작한다”를 확인할 수 있게 끊음.
- **상가 유지**: 상가 관련 코드는 그대로 두고, “주택용”만 추가·분기.

---

## 1단계: Supabase 스키마·테이블

- **할 일**
  - 주택 매물 전용 테이블(예: `listings_housing`) 정의. (주택 매매 / 주택임대차 / 원룸임대차 구분용 컬럼 포함.)
  - 주소·좌표 **공통 테이블**(예: `address_geocode_cache`) 정의. (전체 주소, 위도, 경도 등.)
- **이유**: 동기화·API·지도 모두 “어디에 저장할지”가 먼저 있어야 함.
- **확인**: Supabase 대시보드에서 테이블 생성 여부 확인.
- **적용 파일**: `scripts/supabase_schema_housing.sql` (Supabase SQL Editor에서 실행)

---

## 2단계: Google Sheets → Supabase 동기화 (주택 전용)

- **할 일**
  - 주택 매물장 스프레드시트(시트 ID·시트 이름) 설정.
  - 주택 매매, 주택임대차, 원룸임대차 시트를 읽어서 **변형 없이** Supabase `listings_housing`에 넣는 동기화 로직 구현.
  - 기존 `sheet_to_supabase_sync` 패턴을 참고하되, **주택 전용 시트/테이블**로 분리.
- **이유**: 데이터가 Supabase에 쌓여야 API·지도·필터를 만들 수 있음.
- **확인**: 동기화 실행 후 Supabase에 행이 들어오는지 확인.

---

## 3단계: 주소·좌표 (지오코딩·캐시)

- **할 일**
  - 주택 매물의 "전체 주소(지역2 + 지역 + 지번)"를 기준으로, **주소 공통 테이블**에 좌표가 없으면 지오코딩 호출 후 저장.
  - 동기화 후 배치로 한 번 돌리거나, 동기화 단계에서 "새 주소만 지오코딩"하도록 넣어도 됨.
  - 주택 매물 조회 시 **좌표는 이 캐시 테이블과 조인**해서 사용.
- **이유**: 지도 마커를 붙이려면 좌표가 필요하고, 같은 주소 중복 지오코딩을 막으려면 캐시가 먼저 있어야 함.
- **확인**: 캐시 테이블에 주소·위도·경도가 채워지는지, 주택 매물과 매칭 가능한지 확인.

---

## 4단계: 백엔드 — 주택 매물 조회 API

- **할 일**
  - **주택 전용 조회 API** 추가 (예: `/api/listings/housing` 또는 `?type=housing` 등).
  - Supabase `listings_housing` + 주소 캐시(좌표) 조인해서, **매니저·어드민만** 접근 가능하도록 권한 체크.
  - 쿼리 파라미터로 **매매 / 전세 / 월세** 구분(주택 매매 시트 vs 주택임대차 시트 + 거래유형) 반영.
- **이유**: 프론트에서 "주택 보기"일 때 호출할 엔드포인트가 있어야 함. 상가용 기존 API는 그대로 둠.
- **확인**: 매니저/어드민 계정으로 API 호출 시 JSON으로 주택 매물(좌표 포함)이 오는지 확인.

---

## 5단계: 프론트 — 상가/주택 전환 + 주택 데이터 표시

- **할 일**
  - 상태카운트바에 **상가 / 주택** 버튼 추가. (한 번에 하나만 선택.)
  - **주택** 선택 시 → 위에서 만든 **주택 API** 호출, 응답을 기존 `LISTINGS`/`FILTERED_LISTINGS`와 동일한 방식으로 사용.
  - 주택 선택 시 **매매 / 전세 / 월세** 버튼 표시, 선택 값에 따라 API에 subtype 전달하거나 프론트에서 필터.
  - 받아온 주택 데이터로 **리스트·지도 마커** 표시. (필터는 6단계에서 완성해도 됨.)
- **이유**: "주택 누르면 주택 데이터만 보인다"까지 되면, 데이터 파이프라인과 UI 분기가 한 번에 검증됨.
- **확인**: 상가 선택 시 기존처럼 상가만, 주택 선택 시 주택만 리스트·지도에 나오는지 확인.

---

## 6단계: 프론트 — 주택 전용 필터

- **할 일**
  - 주택 모드일 때만 쓰는 **필터 세트** 적용 (지역, 지번, 지역2, 유형, 건물명, 동, 호수, 향, 공급, 전용, 보증금, 월세, 방, 화장실, 층수, 의뢰인, 연락처, 비고 등).
  - **현황** 필터: 기본값 **생**, 초기화 버튼 눌러도 **생**으로 복귀.
  - 텍스트 복수 검색(쉼표), 층수 -1 단독·범위, 숫자 범위 등 기획대로 적용.
- **이유**: 5단계에서 "데이터는 보임"이 확인된 뒤, "원하는 것만 골라 보기"를 넣는 편이 안정적.
- **확인**: 현황 생/계/완 전환, 초기화 시 생 복귀, 지역·보증금 등 필터 동작 확인.

---

## 7단계: 프론트 — 주택 상세·UX 마무리

- **할 일**
  - 주택 매물 클릭 시 **상세 패널**에 주택 헤더에 맞는 필드로 표시.
  - 필요 시 **브리핑/추천** 등은 "주택 매물 ID"가 상가와 구분되도록 이미 설계된 ID 체계 사용.
- **이유**: 리스트·지도·필터가 된 다음에 상세·부가 기능을 붙이면 흐름이 단순함.
- **확인**: 주택 매물 클릭 시 상세가 정상 표시되는지 확인.

---

## 8단계: 주택 매물등록

- **할 일**
  - **주택 매매**, **주택임대차** 시트에 넣는 **등록 폼** 추가. (상가와 비슷한 양식, 헤더에 맞는 필드.)
  - 등록 시 대상 시트(주택 매매 vs 주택임대차) 선택. 등록 권한은 기획 미정이므로, 일단 매니저/어드민만 또는 기존 상가와 동일하게 두고 나중에 조정 가능.
- **이유**: 조회·필터·지도가 안정된 뒤 "추가" 기능을 넣는 편이 디버깅이 쉽고, 상가 등록 로직과도 분리해 두기 좋음.
- **확인**: 등록 후 해당 시트·Supabase(동기화 후)에 행이 들어오는지 확인.

---

## 진행 순서 요약 표

| 단계 | 내용 | 다음에 필요한 것 |
|------|------|------------------|
| 1 | Supabase 스키마(주택 매물 테이블, 주소 캐시 테이블) | 2 |
| 2 | 주택 시트 → Supabase 동기화 (매매/임대차/원룸) | 3 |
| 3 | 주소 지오코딩·캐시 저장 및 매물과 연결 | 4 |
| 4 | 주택 조회 API (역할: 매니저/어드민) | 5 |
| 5 | 프론트: 상가/주택 전환 + 주택 API 호출 + 리스트·지도 표시 | 6 |
| 6 | 프론트: 주택 전용 필터(현황 기본·초기화=생 포함) | 7 |
| 7 | 프론트: 주택 상세 패널·UX 마무리 | 8 |
| 8 | 주택 매물등록 폼(주택 매매/주택임대차 시트) | — |

---

## 참고

- **상가** 관련 코드(Excel, 기존 `/api/listings`, 상가 필터·매물장 버튼)는 **1~8 단계 모두에서 그대로 두고**, "주택용"만 추가·분기하는 방식이면 기존 동작을 깨지 않습니다.
- 2단계까지 끝나면 "주택 데이터가 Supabase에 있다"를, 4단계까지 끝나면 "API로 주택을 줄 수 있다"를, 5단계까지 끝나면 "화면에서 주택을 볼 수 있다"를 각각 검증할 수 있어서, 단계별로 끊어서 진행하기 좋습니다.
