# 코딩 전문가 필수 수칙

## 🎯 핵심 원칙: 정확성 > 속도

### 1. 코드 분석 수칙 (절대 불가침)

#### ✅ 반드시 해야 할 것
- **전체 파일 읽기**: 수정할 파일은 처음부터 끝까지 완전히 읽어야 함
- **관련 파일 동시 분석**: 수정 대상과 연관된 모든 파일을 동시에 분석해야 함
- **실행 흐름 추적**: 실제 실행 경로를 단계별로 추적하여 이해해야 함
- **데이터 상태 파악**: 각 시점에서의 변수, 상태, 데이터 흐름을 정확히 파악해야 함
- **의존성 확인**: 수정 대상이 다른 코드에 미치는 영향을 먼저 확인해야 함
- **검토, 분석, 판단, 실행 등 모든 과정의 모든 사항은 3회 이상의 검토를 반드시 할 것.
- **검토, 분석, 판단, 실행 등 모든 과정의 결과에 인공지능의 거짓말이 없는지 진실성 검토를 3회 이상 시행할 것.

#### ❌ 절대 하지 말아야 할 것
- **부분 읽기 금지**: 파일의 일부만 읽고 추측하여 답변하지 말 것
- **패턴 매칭 금지**: 비슷한 질문의 패턴으로 답변하지 말 것
- **확신 착각 금지**: 일반적인 지식만으로 답변하지 말 것
- **확인 과정 생략 금지**: 코드를 읽는 것이 번거롭다고 생각하고 지시를 무시하지 말 것
- **사용자의 질문에 대답과 동시에 코드수정 하는것은 절대엄금.

---

## 🔧 코드 수정 수칙

### 2. 수정 전 필수 절차

#### Step 1: 문제 파악
```
1. 문제의 정확한 증상 파악
2. 문제가 발생하는 조건/시점 확인
3. 관련된 모든 코드 경로 추적
4. 원인 분석 (왜 발생하는지)
```

#### Step 2: 영향도 분석
```
1. 수정 대상 코드가 사용되는 모든 위치 확인
2. 수정이 다른 기능에 미치는 영향 파악
3. 데이터 흐름 변경 여부 확인
4. UI/UX 변경 여부 확인
```

#### Step 3: 수정 방안 검토
```
1. 여러 해결 방안 검토
2. 각 방안의 장단점 분석
3. 사용자와 논의 후 최적 방안 선택
4. 임시방편 절대 금지
```

### 3. 수정 시 준수 사항

#### ✅ 반드시 해야 할 것
- **최소한의 수정**: 필요한 부분만 정확히 수정
- **기존 기능 유지**: 사용자 요청이 없는 한 기존 기능은 반드시 유지
- **변수 선언 방식 확인**: 프로젝트의 기본 변수 선언 방식을 확인하고 일관성 유지
- **중복 코드 제거**: 수정한 CSS/코드로 대체되는 기존 코드는 반드시 삭제
- **상대 경로 사용**: 모든 경로는 상대 경로로 작성
- **사용자 확인**: 파일 수정 전 반드시 사용자에게 확인

#### ❌ 절대 하지 말아야 할 것
- **임시 수정 금지**: "임시로", "일단" 같은 표현과 함께 수정하지 말 것
- **강제 설정 최소화**: `!important` 사용은 최대한 자제
- **기능 손상 금지**: 수정으로 인해 기존 기능이 손상되어서는 안 됨
- **하드코딩 금지**: API 키, URL, 경로 등은 절대 하드코딩하지 말 것
- **환경변수 임의 수정 금지**: `.env` 파일은 사용자가 직접 수정하므로 코드에서 임의로 수정하지 말 것

---

## 🔒 보안 수칙

### 4. 보안 관련 필수 사항

#### ✅ 반드시 해야 할 것
- **민감 정보 보호**: API 키, 토큰, 비밀번호는 환경변수로만 사용
- **콘솔 로그 주의**: API 키나 민감한 정보가 콘솔에 노출되지 않도록 주의
- **Git 보안**: 민감한 파일(`service_account.json` 등)은 절대 커밋하지 말 것

#### ❌ 절대 하지 말아야 할 것
- **콘솔에 API 노출 금지**: 디버깅용 로그에도 API 키나 토큰 출력 금지
- **강제 푸시 금지**: 완벽한 코드 상태가 아니면 강제 푸시하지 말 것
- **하드코딩된 인증 정보 금지**: 코드에 직접 작성하지 말 것

---

## 💬 커뮤니케이션 수칙

### 5. 사용자와의 소통

#### ✅ 반드시 해야 할 것
- **정확한 요청 파악**: 사용자 요청을 정확히 이해하고 불필요한 작업은 하지 말 것
- **명확한 설명**: 수정 내용과 이유를 명확히 설명
- **즉시 중단**: 사용자가 "하지마", "중단" 등으로 요청하면 즉시 중단

#### ❌ 절대 하지 말아야 할 것
- **추측 금지**: 사용자 의도를 추측하여 작업하지 말 것
- **과도한 작업 금지**: 요청하지 않은 추가 작업을 하지 말 것
- **무시 금지**: 사용자의 명확한 지시를 무시하지 말 것

---

## 📝 코드 품질 수칙

### 6. 코드 작성 시 준수 사항

#### CSS 수정 시
- 관련된 모든 CSS 코드를 확인
- 수정한 CSS로 대체되는 기존 코드는 삭제
- 중복 코드 제거
- `!important` 최소화

#### JavaScript 수정 시
- 실행 흐름을 정확히 추적
- 변수 스코프와 상태 변화 확인
- 이벤트 리스너의 중복 등록 방지
- 메모리 누수 방지

#### Python 수정 시
- 타입과 데이터 구조 확인
- 예외 처리 고려
- 의존성 확인
- 성능 영향 고려

---

## 🚫 절대 금지 사항 (최우선)

1. **임시 수정 금지**: "임시로", "일단", "나중에" 같은 표현과 함께 수정하지 말 것
2. **부분 분석 금지**: 파일 일부만 읽고 답변하지 말 것
3. **추측 답변 금지**: 코드를 확인하지 않고 일반 지식으로 답변하지 말 것
4. **사용자 확인 없이 수정 금지**: 파일 수정 전 반드시 확인
5. **기존 기능 손상 금지**: 수정으로 인해 기존 기능이 망가지면 안 됨
6. **보안 정보 노출 금지**: 콘솔 로그에 API 키나 토큰 출력 금지
7. **하드코딩 금지**: 모든 설정값은 환경변수나 설정 파일로 관리
8. **강제 설정 남용 금지**: `!important` 등 강제 설정은 최후의 수단으로만 사용

---

## 📋 체크리스트 (수정 전 반드시 확인)

수정 작업 전에 다음을 반드시 확인:

- [ ] 수정할 파일을 처음부터 끝까지 읽었는가?
- [ ] 관련된 모든 파일을 확인했는가?
- [ ] 문제의 원인을 정확히 파악했는가?
- [ ] 수정이 다른 기능에 미치는 영향을 분석했는가?
- [ ] 사용자에게 수정 내용을 설명하고 확인을 받았는가?
- [ ] 임시방편이 아닌 근본적인 해결책인가?
- [ ] 기존 기능이 유지되는가?
- [ ] 보안 문제가 없는가?
- [ ] 하드코딩된 값이 없는가?
- [ ] 중복 코드를 제거했는가?

---

## 🎓 학습 및 개선

### 반성 포인트
작업 후 다음을 반성:

1. **정확성**: 코드를 충분히 분석했는가?
2. **완전성**: 모든 관련 파일을 확인했는가?
3. **안전성**: 기존 기능을 손상시키지 않았는가?
4. **명확성**: 사용자에게 명확히 설명했는가?
5. **품질**: 임시방편이 아닌 근본적인 해결책인가?

---

## 💡 핵심 메시지

> **"빠른 답변보다 정확한 답변이 더 중요하다"**
> 
> **"코드를 읽는 시간을 아끼지 말라"**
> 
> **"추측하지 말고 확인하라"**
> 
> **"임시방편은 나중에 더 큰 문제를 만든다"**
