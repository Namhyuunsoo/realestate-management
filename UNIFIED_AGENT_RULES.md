# 🎯 통합 에이전트 규칙 (UNIFIED AGENT RULES)

이 파일은 이 프로젝트의 모든 코딩 및 소통 규칙을 집대성한 최상위 법전입니다. 에이전트는 작업을 시작하기 전 반드시 이 파일을 정독해야 합니다.

---

## 🚨 [제1장: 부동산 관리 시스템 절대 법언]
1. **1행 = 1매물 원칙**: 구글 시트의 1줄은 무조건 1개의 독립된 매물이다. 
   - 주소, 상호, 담당자가 다 같아도 **행 번호가 다르면 무조건 다른 매물**로 취급하라. 
   - 어떠한 '스마트 중복 제거'나 '매물 합치기' 로직도 사용자 허락 없이 제안/구현하지 마라.
2. **Upsert 기반 동기화**: 모든 매물 데이터는 `Slot ID + Row Index`를 조합한 고유 ID를 사용해 Upsert(덮어쓰기)하라. 절대 전체 삭제 후 삽입하지 마라.
3. **사용자 관찰 우선**: 사용자가 "시트는 깨끗하다"거나 "데이터에 문제없다"고 하면, 시트 오염을 의심하지 말고 코드 내의 '동시성(Race Condition)'이나 '동기화 로직'의 버그를 먼저 찾아라.
4. **기능 임의 추가 금지**: '배포', '공유', '자동 복제' 등 사용자가 명시적으로 요청하지 않은 데이터 흐름 코드는 절대 만들지 마라. 만약 기능의 추가가 권장되거나 필요한 경우, 사용자가 코딩 초보임을 명심하여 기능의 필요성에 대해 충분히 설명하고 반드시 허락을 구하라.

---

## 🛠 [제2장: 코딩 및 기술 수칙]
### 1. 기본 원칙
- **정확성 > 속도**: 빠르게 답변하는 것보다 정확하게 코드를 읽고 분석하는 것이 우선이다.
- **전체 파일 분석**: 수정할 파일은 반드시 처음부터 끝까지 읽어라. 부분 읽기 후 추측은 절대 금지다.
- **실행 흐름 추적**: 실제 데이터가 어떻게 흐르는지 단계를 3회 이상 검토하라.

### 2. 코드 수정 규칙
- **최소한의 수정**: 요청받은 부분만 정확히 수정하고 불필요한 리팩토링은 피하라.
- **기존 기능 유지**: 사용자 요청이 없는 한 기존의 UI와 기능은 반드시 보존하라.
- **환경변수 필수 사용**: 모든 API 키와 설정값은 절대 코드에 하드코딩하지 마라. 반드시 **환경변수**를 통해 접근하라. (Vercel 환경변수 및 로컬 `.env` 파일 활용)
- **상대 경로 사용**: 모든 경로는 상대 경로로 작성하여 이식성을 보장하라.
- **CSS 수칙**: `!important` 사용을 자제하고, 중복된 기존 코드는 삭제하라.

### 3. 환경 및 도구
- **PowerShell**: 터미널 명령은 PowerShell 기반으로 작성하라.
- **Git**: 사용자 확인 없이 멋대로 커밋하지 마라. 구글 시트 관련 엑셀 파일은 커밋에서 제외하라.

---

## 🔒 [제3장: 보안 및 안정성]
- **민감 정보 보호**: `service_account.json` 등은 절대 커밋 금지. 콘솔 로그에 API 키를 노출하지 마라.
- **임시 수정 절대 금지**: "임시로", "일단" 식의 처방은 절대 금지하며, 항상 근본적인 해결책을 제시하라. 임시 생성 파일 및 코드는 복잡성만 가중시키므로 절대 금지한다.
- **구글 시트 보호**: 구글 시트 손상 또는 오염되었다는 성급한 판단을 유보하고, 로직의 결함을 먼저 살펴라.

---

## 💬 [제4장: 커뮤니케이션 스타일]
- **한국어 응답**: 모든 대화와 주석은 한국어로 진행한다.
- **추측 금지**: 의도가 불분명하면 반드시 질문하고, 제멋대로 추측하여 행동하지 마라.
- **즉시 중단**: 사용자가 "하지마"라고 하면 그 즉시 모든 작업을 멈추고 대기하라.

---

## 📂 [제5장: 프로젝트 특성 (Project Context)]
- **부동산 매물/관리 시스템**: 상가 및 건물 매물을 관리하며 시각화하는 전문 시스템.
- **기술 스택**: Python Flask (Backend) + Vanilla JS Module (Frontend) + Supabase (Database).
- **데이터 흐름**: 구글 시트가 원본 소스이며, 웹훅을 통해 Supabase로 실시간 동기화(Upsert)되는 구조.
- **주요 통합**: 
  - Google Sheets API (데이터 소스)
  - Naver Maps API (지도 시각화 및 클러스터링)
  - Vercel (배포 환경)
  - DuckDNS (IP 관리)
- **아키텍처**: 
  - 프론트엔드는 모듈형 JS 구조(`main-new.js`)를 따르며 기능별로 분리됨.
  - 백엔드는 서비스(Services) 계층에서 모든 비즈니스 로직(동기화, 검색 등)을 처리함.
- **타겟 사용자**: 관리자(Admin), 매니저(Manager), 일반 사용자(User) 등 역할(Role) 기반의 권한 시스템 운영.
